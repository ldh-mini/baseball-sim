import React from 'react'
import ReactDOM from 'react-dom/client'
import KBOSimulation from '../kbo-simulation.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <KBOSimulation />
  </React.StrictMode>,
)
